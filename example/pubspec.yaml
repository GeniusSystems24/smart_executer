name: smart_executer_example
description: Example app demonstrating the Smart Executer package.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  smart_executer:
    path: ../
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
